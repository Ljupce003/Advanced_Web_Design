
### Core Vue concepts

#### v-bind
It is used for 

#### v-model
It is used for *two-way binding* between forms or components and the Vue data. Means that it reads a value from the dataand displays it, but also changes the data in Vue when it is changed on the *displaying side* . It is syntatic sugar for *<u>v-bind</u>* 
v-for
data()
computed:

